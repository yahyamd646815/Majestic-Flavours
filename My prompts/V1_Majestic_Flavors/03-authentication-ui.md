Read AGENTS.md first and follow it strictly.

Implement the Sign In screen for the Majestic Flavors inventory app. This is an internal tool for 15 to 25 restaurant staff — there is no public sign-up. Accounts are created by Admins only.

The screen should have:

At the top center: the Majestic Flavors MF crown logo from assets/images via the centralized images import, displayed at a medium size. Below the logo, show the text "Majestic Flavors" in the deep maroon brand color using a bold font, and below that a subtitle in smaller secondary text: "Inventory Management".

Below the header: an email input field and a password input field, both with rounded borders using the brand border color.

A Sign In button using the primary gold color as the background with dark text, full width, rounded.

A small "Forgot password?" text link in the secondary text color below the button.

The overall background should be white or very light cream. The layout should feel premium and match the brand's regal identity without being heavy or dark on a small phone screen.

When the Sign In button is pressed, show a loading spinner inside the button. Do not navigate anywhere yet — auth logic comes in the next step.

Do not implement auth logic yet. UI only.
